// ----- REACT NATIVE -----
import React from "react";
// ----- SCREENS -----
import StatsScreen from "@/src/app/(screens)/StatsScreen";

/**
 * Stats component renders the StatsScreen component.
 * This component is used in the bottom tab navigation structure.
 * It is responsible for displaying the statistics of the user's mood entries.
 */
export default function Stats() {
  return <StatsScreen />;
}
