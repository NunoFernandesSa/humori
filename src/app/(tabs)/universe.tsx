import React, { JSX } from "react";
import UniverseScreen from "../(screens)/UniverseScreen";

/**
 * Universe tab.
 * Displays the universe screen.
 */
export default function Universe(): JSX.Element {
  return <UniverseScreen />;
}
